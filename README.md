# dotfiles

My personal dotfiles. Install them by cloning the repository as `.dotfiles` in your home directory and run the bootstrap script.

```shell
git clone git@github.com:barthjs/dotfiles.git .dotfiles
cd .dotfiles
./bootstrap.sh
```
